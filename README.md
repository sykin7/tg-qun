```
BOT_TOKEN: (来自 @BotFather)
ADMIN_USER_ID: (来自 @userinfobot，您的个人数字ID)
GROUP_MAP: 您的群组地图，例如: g1:-100123456,g2:-100789012

把您的机器人拉到您所有的目标群组（例如 g1 群和 g2 群）。
在 g1 群里发 /getid，获取到 g1 的ID (例如 -100123456)。
在 g2 群里发 /getid，获取到 g2 的ID (例如 -100789012)。

私聊您的机器人：
发送 /start。机器人应该会回复您，并列出 g1 和 g2 两个别名。
发送 /set g1。机器人回复：“✅ 已切换... g1”。
发送一张图片 (可以带说明，也可以不带)。
（查看 g1 群）：机器人（用您伪装的名字）在群里发了这张图片。
发送纯文本 这是g1的测试。
（查看 g1 群）：机器人发了 这是g1的测试。
发送 /set g2。机器人回复：“✅ 已切换... g2”。
发送一个文件。
（查看 g2 群）：机器人发了这个文件。
发送 /who。机器人回复：“ℹ️ 当前消息目标群组是: g2”。
发送 /to g1 这是一条给g1的临时文本。
（查看 g1 群）：机器人发了 这是一条给g1的临时文本。
发送 /who。机器人仍然回复：“ℹ️ 当前消息目标群组是: g2”。(因为 /to 不会改变模式)。
发送 /set none。
发送 这条消息不会被转发。
（查看 g1 和 g2 群）：什么也没发生。
